[project]
name = "rate-limiter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.139.0",
    "httpx>=0.28.1",
    "redis>=8.0.1",
    "requests>=2.34.2",
]
